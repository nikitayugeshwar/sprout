{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": {
        "//": "The WHO tables are read with fs.readFileSync from a path built out of import.meta.url. Vercel's static tracer cannot see that, so without includeFiles the JSON is left out of the bundle and every growth request dies on ENOENT.",
        "includeFiles": "src/data/who/**"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "api/index.js" }]
}
