# Base URL of the Sprout API, including the /api/v1 prefix.
# Local development against `npm run dev:api`:
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# In production, point this at your deployed API, e.g.
# NEXT_PUBLIC_API_URL=https://sprout-api.onrender.com/api/v1
